package;
import flash.display.Sprite;

/**
 * ...
 * @author Tara Moses
 */
class Castle_Blocks extends Sprite 
{
	public var sprite:Sprite;
	
	public function new(x:Int, y:Int, w:Int, h:Int) 
	{
		super();
		
		/*//create dynamic block
		block = Main.game.createBox(x, y, w, h, true, 1);
		
		//create sprite
		if () blockIconName = "verticalBlockIcon.png";
		else blockIconName = "horizontalBlockIcon.png";
		
		var blockIcon = new Bitmap(Assets.getBitmapData("img/verticalBlockIcon.png"));
		sprite = new Sprite();
		sprite.addChild(blockIcon);
		sprite.x = -blockIcon.width / 2;
		sprite.y = -blockIcon.height / 2;
		this.addChild(sprite);
		
		//put sprite on screen
		this.x = x;
		this.y = y;*/
	}
	
}